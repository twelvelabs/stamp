Name: file

values:
  - key: FileName
    type: string
    default: untitled.txt

  - key: FileContent
    type: string
    default: ""

tasks:
  - type: create
    src: "template.txt"
    dst: "{{ .FileName }}"
